# Repository Guidelines

## Project Overview
Personal brand landing page for **danyzmaj** (deployed at `https://www.danyzmaj.com/`). A single self-contained HTML file renders an animated SVG logomark on a dark "letterpress" background. No application logic, no backend.

## Architecture & Data Flow
- Zero-dependency static site: one HTML document with inline `<style>`, inline SVG, and one tiny inline `<script>` (a single click listener). No third-party requests. Favicons and sharing images are served as local static assets so browsers and search crawlers can fetch them.
- **Tap-morph architecture** (branch `feat/dragon-scroll-logo`): tapping/clicking the mark toggles it between the Z monogram and a top-down dragon. The whole page is a `<button class="trigger">` wrapping the SVG; JS only toggles `.awake` on `<html>` and mirrors it to `aria-pressed` — no per-frame JS.
- The choreography itself is pure CSS: `--p` is a registered custom property (`@property --p{syntax:'<number>'}`) that CSS-transitions 0→1 (`.awake`) or 1→0 on the toggle. Sub-timeline ramps (`--p1` straighten, `--ph` head flip, `--p2` wings, `--p3` fire) are `clamp(0,calc(…),1)` windows of `--p`, defined on the `svg` and recomputed live every animation frame as `--p` interpolates — this reactive recompute is what makes a single transitioned number drive a multi-beat sequence.
- Forward and reverse use different tokens: `html{transition:--p 1400ms var(--reveal)}` for the awakening (evenly-paced `cubic-bezier(0.4,0,0.2,1)` so all four beats stay legible — do NOT reuse the punchy `--ease` token here, it front-loads the whole show into the first 300ms), `html:not(.awake){…var(--recoil)}` for a quicker back-loaded snap home. Re-verify pacing by sampling `getComputedStyle(document.documentElement).getPropertyValue('--p')` at several `setTimeout` points after a click if you touch these curves — don't judge by end-state screenshots alone.
- SVG is split into anatomical groups — `#spine`, `#head` (`.side` profile / `.crown` top-down layers), `#tail`, `#wings` (`.fold`), `#spark`/`#fire` — each animated with `calc()`-interpolated CSS transforms (`transform-box:view-box`, absolute `px` origins). Transforms + opacity only; the world is ink/bone die-cut, so NEVER cross-fade with alpha — reveals use scale collapse/expand (e.g. the head's profile→bird's-eye card-flip).
- At `--p:0` the mark must render pixel-identical to the original monogram; path data is duplicated in `favicon.svg` and its PNG/ICO exports and must stay in sync with the `.side`/skeleton paths.
- Layout: unchanged from the original single static viewport (`body` grid, `overflow:hidden`, no scroll container) — the interaction lives entirely in the button/CSS, not in page height.
- SVG layering (paint order): wings → tail → spine → head → spark; `.body` (bone) with `.hole` (ink cutouts), `.acc` (ember accent).

## Key Directories
- `index.html` — the landing page (the animated logomark; everything below describes it).
- `<project>/` — per-app static pages for App Store listings, following the pattern
  `/<project>/` (support page, contact email) and `/<project>/privacy/` (privacy policy).
  Currently: `holdup/`, `pixelpup/`, `pixelmew/`, `vedro/`, `evido/`. Each page is a self-contained HTML
  file with its own local favicon set. Hold Up reuses the homepage's `--ink`/`--bone`/`--ember`
  tokens and mono plate type; PixelPup, PixelMew, vedro, and Evido instead define their own brand palette
  and typography drawn from each app's real icon colors — copy an existing project's pair as a
  starting point, but match the app's own identity rather than forcing the dragon brand.
- `tools/sync-art.py` — pulls product art and catalogues from the sibling app repos (`../pixelmew`, `../pixelpup`) into this one. Dev-time only; needs Python 3 and Pillow. The deployed site stays dependency-free.
- `.gitignore` — ignores Python bytecode, `.DS_Store`, `.worktrees/`.

## Development Commands
No build, lint, or test tooling. Preview locally:
```sh
open index.html                 # macOS, file:// is sufficient
python3 -m http.server 8000     # or serve over HTTP
```
Refresh or audit the art copied from the app repos:
```sh
python3 tools/sync-art.py --check   # report drift, write nothing, exit 1 if stale
python3 tools/sync-art.py           # rewrite the copies, then read `git status`
```

## Change Pipeline
Follow the route that matches the change; each ends at a deploy.

1. **Page markup, copy, CSS, inline script (any product page or the homepage)** — edit the file here. Nothing to generate, nothing to sync. This includes PixelMew's page: the PixelMew repo's `brand/page.html` is a design study, not an upstream.
2. **Product artwork or catalogue (a new cat/dog, new palette, moved scenery, new sprite)** — change it in the app repo (`../pixelmew`, `../pixelpup`), run that repo's generators and its `verify.py` where it has one, then come back here and run `python3 tools/sync-art.py`. Never hand-edit a synced file; `--check` will just flag it again.
3. **A new synced file or a new product** — add a step to `MANIFEST` in `tools/sync-art.py` (ops: `copy`, `downscale`, `icons`, `json`, `embed`), then run the script. Hand-copying is what the manifest exists to prevent.
4. **Deploy** — commit and push `main`; GitHub Pages builds in about a minute. Assets are served with a ten-minute CDN TTL (`cache-control: max-age=600`), so a stale live response right after a push is the cache, not a failed deploy. Confirm with a fresh fetch after the TTL, not with a query string.

Before committing anything: `python3 tools/sync-art.py --check` must print `in sync`, and a changed page must be exercised in a browser over `http://localhost:8000` (click the thing that changed, then confirm no-JS still renders). The two watch-face pages must stay section-for-section and action-for-action identical.

## Code Conventions & Common Patterns
- **Everything inline**: keep CSS in the `<head>` `<style>` block and SVG inline; do not split into separate files or add dependencies.
- **Design tokens** live as CSS custom properties on `:root` with poetic comments:
  `--ink:#171210` (background), `--bone:#F2EDE4` (mark), `--ember:#FF6A1F` (accent), `--ease` (620ms entrance flourish), `--reveal`/`--recoil` (tap-morph in/out curves). Reference tokens via `var(--…)`; never hardcode these colors elsewhere. `theme-color` meta mirrors `--ink`.
- **Compact CSS style**: no spaces after `:` or around `{}`, one rule per line where short.
- **Accessibility is deliberate**: the `<h1>` lives in `#plate`, permanently visible on BOTH pages (ink and paper) — the two themes must stay element-for-element identical, differing only in the swapped `--ink`/`--bone` values; the `svg` is `aria-hidden`, `#fx`/`#lure` are `aria-hidden`, and `prefers-reduced-motion` freezes the page entirely: the CSS query collapses all animations to 1ms (no idle blink/lure) and `push()` bails before doing anything, so scroll/keys never trigger the rampage or the theme flip — the mark just stands still. Preserve all of these when editing.
- **Flat means flat — no filters, ever**: the mark carries no drop-shadow in either theme (a shadow darkens the page around the mark and makes the painted `.hole` overlays visibly mismatch the background). Holes are painted `var(--ink)` and must be indistinguishable from the page bed in both themes.
- **Animation**: the rampage is a `wake` keyframe on `html.rampage` scrubbed by JS (`currentTime` driven from scroll); idle life runs on its own clock — `pulse` (`.acc`), `blink` (`.eye`), `lure` (`#lure`). **Any animation that must not be scrubbed by the rampage timeline MUST be added to the exclusion regex in the script** (`/^(pulse|blink|lure)$/`), or the scroll will hijack its clock. Idle motion must be discrete and legible (a blink, a falling ember) — NEVER slow sub-pixel scaling of the hard-edged mark, which reads as pixel shimmer, not life.
- **The eye winding trap**: the side-head eye is drawn ONLY by the `.hole.eye` overlay path — it is deliberately NOT wound into the `.body` path (unlike the mouth line, which is both) so the `blink` scale-collapse reveals bone, not a wound hole. Don't "re-sync" the eye subpath back into the body path.
- Viewport sizing uses `min-height:100svh` with `100vh` fallback and `min(58vmin,620px)` for the mark — keep the fluid-with-cap pattern.

## Important Files
- `index.html` — entry point, styles, markup, favicon: the whole project.

## Runtime/Tooling Preferences
- No runtime, package manager, or framework — and none should be introduced. Changes must keep the page a single dependency-free HTML file.
- `.gitignore`'s Python entries only anticipate ad-hoc local scripts (e.g., `http.server`); there is no Python code in the repo.

## Testing & QA
- No test framework. QA is visual: open the page, verify the mark at rest matches the original monogram exactly and sits rock-still (idle life is only the eye blink every ~6.4s, the ember lure drifting at the bottom, the `danyzmaj` wordmark beneath), scroll and watch the full rampage (Z reassembles into the dragon → liftoff → charge → the page burns to paper), then scroll again to burn back. After each settle, diff the two pages mentally: same elements, same positions, only the ink/bone colors swapped — and the eye/mouth holes must blend seamlessly into the background in both.
- Also verify: keyboard (ArrowDown/PageDown drive the timeline, ArrowUp/PageUp retreat), stopping mid-scroll (the untended fire dies back down and disarms cleanly), no-JS (static Z), reduced-motion (scroll and keys do nothing — the page stays the static ink monogram, name legible), and mobile/desktop centering.
- When touching SVG paths, update both the inline `<svg>` and `favicon.svg`, then regenerate the PNG/ICO icon sizes.

## Projects Gallery & Product Pages
- The homepage's `Projects / 07` link opens `#projects`, a CSS `:target` screen sliding over the dragon. The two-digit number in that link and in the `SELECTED PROJECTS` heading is the card count, so bump both whenever a card is added or removed. Seven cards sit in the grid; six link to a page (Hold Up, PixelPup, PixelMew, Vedro, Shipyard, Helm) and Evido does not. Those links work without JS; hash changes stop any active rampage, manage focus, and update `aria-expanded`. Escape closes the gallery. The overlay itself is the scroll container (sticky `.projects-head` keeps Back reachable); the homepage behind it remains a fixed stage.
- `push()` ignores input while the gallery is open. Rampage scrubbing selects `CSSAnimation` objects only, so gallery transitions keep their own clock. Reduced motion opens the gallery instantly.
- Cards for work with no public page are `<div class="project project-soon">`: no arrow glyph, a dimmed title, an ember `COMING SOON` suffix on the category line, and no hover response (the straighten rule is scoped to `a.project`, so a tilted card that never reacts signals "not openable"). Evido is the only card in this state; `evido/` and its privacy page still exist in the repo but are deliberately unlinked, so do not re-link them without being asked.
- Every card leans away from the page center: `.project{--tilt:7deg}` with `.project:nth-child(odd){--tilt:-7deg}`, so the left column tilts left and the right column tilts right, and openable cards straighten to `rotate(0)` on hover or keyboard focus. Previews sit at `width:80%` so a 7° swing never clips the card edge. `prefers-reduced-motion` drops both the tilt and the transition.
- Project previews are self-contained: the PixelPup dog is an embedded copy of `pixelpup/assets/dog_labrador.png`; its landscape is CSS. PixelMew matches that pattern deliberately, since the two are sibling watch faces: `.mini-mew` is the same CSS watch as `.mini-watch` (circle, `10:08`, one floor band) in PixelMew's plum/cream/`--mew-box` tones, with an embedded copy of `pixelmew/assets/cat_black_shorthair.png` (the watch's own 466 px sprite). Both embedded sprites are rewritten by `tools/sync-art.py`, so refresh them with the script rather than by hand. Hold Up uses Apple’s official iPhone 17 landscape bezel, embedded on the homepage and stored at `holdup/assets/iphone-17-black-landscape.png` on the product page; those two copies are still manual. Vedro and Evido cards are `<img>` of each product page's own `assets/card-preview.svg`, so the card and the page never drift. PixelMew's `assets/social-preview.png` is composed from the app's real 360 px face renders (`store/screen-cardboard-club-day-360.png` and `screen-nightlight-night-360.png` in the PixelMew repo), so re-compose it rather than retouching if the faces change. Preview product colors stay fixed across the parent ink/paper themes.
- `holdup/index.html` is a product landing page with support at `#support` and the existing privacy link. Its small sign demo fits editable text into a fixed sign and offers three preview backgrounds; this is a browser preview, not the full app. Keep the static sign and product copy usable without JS.
- PixelPup offers twenty named dog breeds in the same order as the watch app. Its `pixelpup/assets/dog_*.png` sprites are copies of `watchface/resources/drawables/dog_*.png`, generated by `tools/gen_assets.py` in the PixelPup app repo. Regenerate there, then run `python3 tools/sync-art.py` here; never edit the site sprites.
- `pixelpup/index.html` contains the existing product playground. Its beta status and release links are maintained there. It and `pixelmew/index.html` are sibling watch-face pages and must keep the same sections and the same actions (hero preview with a breed/cat select, three palette swatches, a day/night toggle; a clickable gallery of every animal; clickable light and dark cards in the background/room section; a stats list with FAQ; a closing contact) — only the art, palette, and product wording differ. In both, the gallery and the light/dark cards are `<button>`s that ship `disabled` and are enabled by the page script, selecting one updates the hero preview and scrolls to it, and the select and the buttons stay in sync through one `sync` pass, so no-JS visitors still get the default face and all copy.
- The two watch faces point at each other: a `.sibling` chip in the header nav, just left of the contact button, carrying the other product's mark, its name and an `↗`. PixelMew links `/pixelpup/`, PixelPup links `/pixelmew/`, and each footer lists the other as well. Below 760 px the `.sibling-name` span is hidden and the chip becomes icon-only, so the header still fits a 360 px phone; the `aria-label` carries the full name either way. Add the same pair of links to any future sibling page rather than leaving one page a dead end.
- **Ownership: this repo owns every product page; the app repos own the art and the catalogues.** Page markup, copy, CSS and inline scripts are edited here, PixelMew's page included — its origin in `../pixelmew/brand/page.html` is now a design study, not an upstream, so do not round-trip edits through the app repo. What is copied in is declared in `tools/sync-art.py`'s `MANIFEST`: PixelMew's `brand/page.{css,js}` + `mark.svg`, its 32-file `faces/*.svg` layer bundle (per theme a room and a readings layer, plus one sprite per cat — the preview stacks three `<img>`s rather than shipping 120 pre-composed faces), the twenty `cats/*.png` (downscaled to 312 px, nearest-neighbour, from the 1254 px originals), its `assets/` icon set (rasterized from `brand/mark.svg` onto a cream `rx=7` bed), the `#catalog`/`#theme-data` JSON blocks in `pixelmew/index.html` (from `cats/catalog.json` and `themes.json`), PixelPup's `dog_*.png` sprites, and the two homepage card sprites embedded as base64. Add a step to that manifest rather than hand-copying a file; the script refuses to run when a declared source is missing. Deliberately unsynced and site-owned: PixelPup's dog-less palette SVGs and its hand-drawn paw icon set, plus every `assets/social-preview.png`.
- Caveat to the rule above: PixelMew's page is styled by `pixelmew/brand/page.css`, which **is** synced, so CSS-only changes to that page (the `.sibling` chip, for example) are made in `../pixelmew/brand/page.css` and pulled in with `tools/sync-art.py`; mirror the matching markup into `../pixelmew/brand/page.html` and re-run its `build_gallery.py` so the design study does not rot. Markup, copy and the inline script of that page are still owned here. PixelPup's page has no such split: CSS and markup both live in `pixelpup/index.html`.
- `vedro/index.html` and `evido/index.html` are product pages: hero, capability list, `#support`, and a privacy companion. Each owns a palette drawn from its app icon (vedro mineral sky + navy, Evido deep teal + mint) and an authored `assets/card-preview.svg` (1000 × 700) reused as the homepage card.
- `vedro/assets/screen-plan.webp` and `screen-drive.webp` are real screenshots of the Flutter app, captured on the iOS simulator with Maestro (`maestro test` flow; Maestro needs `JAVA_HOME=/opt/homebrew/opt/openjdk`, and `debugShowCheckedModeBanner:false` was set in the app repo only for the capture, then reverted). Re-capture after app UI changes instead of retouching the images, and keep the captions' numbers matching what the screenshots actually show.
- Vedro's departure comparison is the page's only script: native buttons swap a labelled sample verdict and write a sentence into a polite live region. The buttons stay hidden until the script sets `.js`, so without JS the section keeps one legible default verdict and drops the "choose a departure" instruction. Sample data must stay marked illustrative and must obey the app's worst-wins rule (a bar containing a warn stretch is never labelled Clear).
- `evido/index.html` ships no script: the Worker/Owner walkthrough is radio inputs plus `:has()`, with `.panel-worker` shown by default so engines without `:has()` still render a screen. `evido/assets/screen-worker.png` and `screen-owner.png` are real app screenshots; the walkthrough copy must only describe elements those images actually contain.
- `shipyard/index.html` and `helm/index.html` are pages for unreleased developer tools, so they carry no privacy companion and no download: Shipyard is a graphite machine room built on its real `lib/ui/theme.dart` constants, quoting the actual build and `xcrun altool` commands as proof, and Helm is a plum-and-brass control room whose only light surface is a truthful rendering of its real zinc UI. Both state their status plainly (Shipyard: macOS, in development, not publicly distributed; Helm: personal, self-hosted, no public sign-up, and dependent on Supabase, GitHub, and OpenAI). Each embeds its own `assets/card-preview.svg`, which doubles as the homepage card.
- Shipyard's icon set under `shipyard/assets/` is derived from the app's own `macos/Runner/Assets.xcassets/AppIcon.appiconset/app_icon_1024.png` (the blue container-ship mark), the same way Vedro and Evido use theirs; regenerate it from that PNG if the app icon changes. Helm is the one product page still using the root dragon icons, because its only icon asset is the unmodified SvelteKit default; its header mark is a CSS-drawn brass `H` chip, not the dragon, so give it a real local set as soon as the app has a mark of its own.

## Public Copy
- Use direct, specific descriptions of the apps. Avoid em dashes, repetitive slogan pairs, forced dog puns, and vague claims like “your stats, your way”.
- Review browser titles and social metadata alongside visible text. Preserve factual product behavior and the meaning of privacy and terms pages when editing wording.

## Browser and Search Icons
- Use fetchable icon files, not data URIs. Root `favicon.svg`, `favicon.ico`, `favicon-96x96.png`, `icon-192x192.png`, and `apple-touch-icon.png` share the existing dragon artwork on a square ink background. Hold Up pages use these brand icons.
- PixelPup, PixelMew, vedro, and Evido pages keep their own icon (paw / cat head / route mark / E monogram) with the equivalent files under `pixelpup/assets/`, `pixelmew/assets/`, `vedro/assets/`, and `evido/assets/`. Like PixelPup's paw, PixelMew's set is the page's own brand mark as vector: `favicon.svg` is `pixelmew/brand/mark.svg` on a cream `rx=7` bed and the PNG/ICO sizes are rasterized from those same paths, so the browser tab matches the mark in the page header and the mark shown when the PixelMew repo's copy is opened over `file://`. Regenerate from `brand/mark.svg`, not from the watch's `launcher_icon.png`.
- Each page declares ICO, PNG, SVG, and Apple touch fallbacks. The hostname homepage provides Google's site-wide search favicon; product subdirectories do not get separate Google search icons.
- Homepage and Hold Up share images are `assets/social-preview.png` and `holdup/assets/social-preview.png` (1200 × 630); vedro, Evido, and PixelMew use the same dimensions at `vedro/assets/social-preview.png`, `evido/assets/social-preview.png`, and `pixelmew/assets/social-preview.png`. PixelPup keeps its existing 1440 × 720 image. Keep Open Graph metadata dimensions aligned with the files.
- Google can take days or weeks to recrawl icon changes. Do not keep changing icon URLs to force refreshes.
